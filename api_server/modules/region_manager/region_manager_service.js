// provides the region manager service to manage concerns related to different region hosts

'use strict';

class RegionManagerService {

	constructor (options) {
		Object.assign(this, options);
	}

}

module.exports = RegionManagerService;